The Re-Mote WebServices
=======================

This package contains the files for building and deploying the
webservice components used by Re-Mote.

It has the following structure:

  wsdd		The web service deployment descriptors.
  src		Java classes responsible for the server specific
		part of the services.
  lib		Jar files for generating client stubs and dependencies
		for the client and server.
