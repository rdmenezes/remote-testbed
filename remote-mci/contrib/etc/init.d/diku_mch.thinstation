#!/bin/sh
#
# diku_mch	Starts DIKU Mote Control Host on Thinstations

. $TS_GLOBAL

case "$1" in  
init)
	if ! pkg_initialized $PACKAGE; then
		diku_mch --config-file=/etc/diku_mch.cfg --daemonize=1
		pkg_set_init_flag $PACKAGE
	fi
	;;
help)
	echo "Usage: $0 init"
	;;
*)
	exit 1
	;;
esac

exit 0
