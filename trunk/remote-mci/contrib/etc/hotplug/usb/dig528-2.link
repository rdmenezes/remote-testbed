DONE=0;
while [ "$DONE" = "0" ];
do
HOSTRUNNING=`/bin/ps -A | /bin/grep mci_diku_host`
if [ "$HOSTRUNNING" != "" ]; then
	ln -s /etc/hotplug/usb/dig528-2.remove $1;
	#Send the plug event to the motehost pipe
	/sbin/plugpiper /var/run/motehost.events;
	DONE=1;
else
	sleep 20;
fi
done
